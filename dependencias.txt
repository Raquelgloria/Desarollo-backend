**Queda pendiente Jupoyter

pip install transformers
pip install datasets
pip install sentencepiece
pip install transformers torch datasets
pip install python-utils
pip3 install torch torchvision torchaudio
pip install pandas 
pip install torch
pip install tqdm